import Posts from "../organisms/Posts";
import PageTemplate from "../templates/PageTemplate";

const Home = () => {
  return (
    <PageTemplate>
      <Posts />
    </PageTemplate>
  );
};

export default Home;
