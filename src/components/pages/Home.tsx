import Posts from "../organisms/Articles";
import PageTemplate from "../templates/PageTemplate";

const Home = () => {
  return (
    <PageTemplate>
      <Posts />
    </PageTemplate>
  );
};

export default Home;
